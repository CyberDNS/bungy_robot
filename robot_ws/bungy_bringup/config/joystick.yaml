joy_node:
  ros__parameters:
    device_id: 0
    deadzone: 0.1  # Increased from 0.05 to reduce drift from stick noise
    autorepeat_rate: 20.0


teleop_node:
  ros__parameters:
    
    axis_linear:  # Left thumb stick vertical
      x: 1
    scale_linear:
      x: 0.3  # Reduced from 0.5 for finer control
    scale_linear_turbo:
      x: 0.8  # Reduced from 1.0

    axis_angular:  # Left thumb stick horizontal
      yaw: 0
    scale_angular:
      yaw: 1.5  # Reduced from 2.0 for finer turning control
    scale_angular_turbo:
      yaw: 2.5  # Reduced from 3.0

    require_enable_button: false
    enable_button: 6  # Left shoulder button
    enable_turbo_button: 7  # Right shoulder button